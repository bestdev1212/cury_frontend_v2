import { styled, Stack, Button, Typography } from '@mui/material';

export const NF3CounterBtn = styled(Button)`
    width: fit-content;
    height: 38px;
    padding: 0 16px;
    background: #ffca21;
    font-size: 14px;
    font-weight: 600;
    color: black;
    &:hover {
        background: #ffca21;
    }
`;
